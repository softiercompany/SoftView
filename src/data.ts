import { Video, LearningPath } from './types';

// Demo data removed per request. Connected to Supabase live database!
export const initialVideos: Video[] = [];

export const learningPaths: LearningPath[] = [];
